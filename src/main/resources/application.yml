spring:
  datasource:
    username: axioma
    password: bB7QoVDV9QOm
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://192.168.50.56:5432/axi4utf?stringtype=unspecified&currentSchema=axioma&useUnicode=yes&characterEncoding=UTF-8
  jpa:
    show-sql: true
    #hibernate:
      #ddl-auto: update
  output:
    ansi:
      enabled: always
  task:
    execution:
      pool:
        max-size: 16
        queue-capacity: 100
        keep-alive: "10s"
  main:
    banner-mode: off

redis:
  url: "redis://localhost:6379"
  user:
  password:
  clustered: false


logging:
  file:
    name: logs/cache-service.log
    max-size: 10Mb
    max-history: 7
#  logback:
#    rollingpolicy:
#      file-name-pattern: application.%d{yyyy-MM-dd}.%i.log
#      max-file-size: 10KB
#      max-history: 10
  level:
    root: info
    org.springframework.web: debug
    org.hibernate: error